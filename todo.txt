DOC:
    add note on broadcasting in primal_ops.hpp
    rework gradient comments

REFACTOR:
    rework tensor_view
    add conversion operator
    make functions take tview where possible (strides, functions)
    make tensor constructors foolproof
    actualize combine_flexible signature
    actualize print_arr
    actualize grad_func and val_func signatures
    change transpose to noop
    code duplication in: binaryOps, unaryOps, strides

FEAT:
    add overloads for tensor (size/len, begin, end, data)
    make members private

PERF:
    make stride function transpose dimensions so biggest is highest

DOC:
    add definitions (operation=operation on in/out tensors, operator=operation on nodes)
