[ TODO ]

FIX:

REFACTOR:
    mark functions as const and noexcept
    remove tensor_view from strides
    get rid of c_shape member in Node_batch_matmul
    make sure perm assumes correct dimensions if not given (and add comment)
    (?) change Tensor constructors to factory functions
    disallow iteration semantics and maybe make begin and end private
    (?) remove shape_end and shape_begin
    comment node_type funciton
    rename node Inputs


FEAT:
    label member for nodes
    make Node and pointer access impossible
        make operators return const Pointers (maybe Node_ptr)
        make Node members private (and const from outside)
        make computation_graph members private (and const from outside)
    make sure reset is called automatically (or add in comment)
    make getGradient not use std::fill(,,1) but add 1


PERF:

DOC:
    fix all include lists
    get rid of non-ascii chars
    add doxygen groups


Repo Layout:
kaad/
├─ CMakeLists.txt
├─ include/
│  └─ kaad/
│     ├─ tensor/
│     │  ├─ tensor.hpp              # public API
│     │  └─ ...                     # other public headers
│     ├─ computation_graph/
│     │  ├─ graph.hpp               # public API
│     │  └─ node_handle.hpp
│     └─ tensorfuncs/
│        ├─ adjoint_ops.hpp         # template-heavy, stays in headers
│        └─ ...                     # more .hpp/.inl templates
├─ src/
│  ├─ tensor/
│  │  ├─ tensor.cpp                 # non-template definitions moved here
│  │  └─ ...
│  └─ computation_graph/
│     ├─ graph.cpp
│     └─ ...
├─ examples/
│  └─ outer_product.cpp
├─ tests/
│  └─ ...
└─ cmake/
   └─ kaadConfig.cmake.in           # optional, for install/package

    Add impl files for succifciently large header functions (~50-100 LOC)

LATER:
    make Tensor variant with compile time shape
    make stride function transpose dimensions so biggest is highest
