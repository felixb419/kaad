REFACTOR:
    use std::vector instead of raw pointers (and use rule of 0)
    rework tensor constructors
    make sure tView is even needed

PERF:
    make stride function transpose dimensions so biggest is highest
