[ TODO ]

FIX:

REFACTOR:
    get rid of #ifdef in kernels
    add input checking in kernels

FEAT:
    safe division Kernel (maybe others too)
    make Tensor::linspace and Tensor::sequential iterate strdide-aware
    make stride-aware iterator type
    maybe rename tensorfunc params
    maybe rename tensorfunc 


PERF:

DOC:
    roughly comment metadata functions
    improve comments for shape(), stride()
    replace all "value array" with "element array" (make clear which arguments are copied and which are owned)
    fix all include lists
    get rid of non-ascii chars
    add doxygen groups


Repo Layout:
kaad/
├─ CMakeLists.txt
├─ include/
│  └─ kaad/
│     ├─ tensor/
│     │  ├─ tensor.hpp              # public API
│     │  └─ ...                     # other public headers
│     ├─ computation_graph/
│     │  ├─ graph.hpp               # public API
│     │  └─ node_handle.hpp
│     └─ tensorfuncs/
│        ├─ adjoint_ops.hpp         # template-heavy, stays in headers
│        └─ ...                     # more .hpp/.inl templates
├─ src/
│  ├─ tensor/
│  │  ├─ tensor.cpp                 # non-template definitions moved here
│  │  └─ ...
│  └─ computation_graph/
│     ├─ graph.cpp
│     └─ ...
├─ examples/
│  └─ outer_product.cpp
├─ tests/
│  └─ ...
└─ cmake/
   └─ kaadConfig.cmake.in           # optional, for install/package

    Add impl files for succifciently large header functions (~50-100 LOC)

LATER:
    make Tensor variant with compile time shape
    make stride function transpose dimensions so biggest is highest
    (?) label member for nodes
    (?) put reset, evaluate and getGradient into single function
