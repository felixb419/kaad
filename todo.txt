[FIX]
            make value_mut only callable if node is input

[REFACTOR]
        (?) move operation_concep to operations
        rename strided<BrodCastPolicy> to Broadcast

[FEAT]
(operations) operation names in kernels
(tensor)    Tensor(std::vector<Scalar> &&)
(tensor)    Tensor::size(size_t i);
            StaticVector::MAX_SIZE
(tensor)    Tensor::MAX_RANK

[PERF]
(graph)     arena allocation

[DOC]
            put term definitions in the header class comment (axis, extent, ...)

            Rework kaad.hpp mainpage

            Add Pages for:
                what is backward autodiff
                what is a computation graph
                Node classes
                operators (broadcasting)
                Tensors
                    note that rand and randn require a working std::random_device failing fatally otherwise

[CHORE]

[BUILD]
            Install support
        (?) kaadConfig.cmake.in

[LATER]
(operations) make operations use std::mdspan as soon as available
(graph) (?) Add system for inference
(tensor)    make Tensor variant with compile time shape
(tensor)(?) combine shape and stride into shape struct
(graph)     make stride function transpose axes so biggest is highest
(graph) (?) label member for computation nodes
(graph) (?) put reset, evaluate and getGradient into single function
